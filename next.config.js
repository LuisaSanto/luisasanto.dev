/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    assetPrefix: "/",
    basePath: "",
    images: {
      unoptimized: true,
    },
  };  

module.exports = nextConfig
